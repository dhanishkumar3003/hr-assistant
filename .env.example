# ---------- Database ----------
DATABASE_URL=postgresql://hr_user:hr_pass@localhost:5432/hr_assistant_poc

# ---------- LLM ----------
LLM_API_KEY=
LLM_BASE_URL=

# ---------- LLM ----------
LLM_PROVIDER=ollama
LLM_MODEL=gpt-oss
LLM_API_KEY=

# ---------- Embeddings ----------
EMBEDDING_PROVIDER=ollama
EMBEDDING_MODEL=nomic-embed-text

# ---------- Ollama (shared host for LLM + embeddings) ----------
OLLAMA_BASE_URL=http://localhost:11434
# ---------- Speech-to-Text ----------
STT_API_KEY=
STT_PROVIDER=

# ---------- Microsoft Graph API (email) ----------
GRAPH_CLIENT_ID=
GRAPH_CLIENT_SECRET=
GRAPH_TENANT_ID=
GRAPH_SENDER_MAILBOX=

# ---------- Backend App ----------
ENV=local
SECRET_KEY=changeme
CANDIDATE_TOKEN_EXPIRY_MINUTES=4320

# ---------- Frontend (must be prefixed NEXT_PUBLIC_ to reach the browser) ----------
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# ---------- Docker Compose port overrides ----------
POSTGRES_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=3000